import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register | Provident Groups",
  description: "Create an account with Provident Groups for education and admission support.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
